
  # Portfolio Landing Page

  This is a code bundle for Portfolio Landing Page. The original project is available at https://www.figma.com/design/1FZdR3zYK4AGuC5q6BdpZu/Portfolio-Landing-Page.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.

  npm run build ; npm exec -- netlify-cli@latest deploy --prod
  